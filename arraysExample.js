console.log( "Arrays Example" );

let bagel = [ 'bacon', 'lettuce', 'tomato' ];
//all STRINGS, inside square braces so system knows it's an array, all split by
//commas and there's not trailing comma
//DELINEATED by commas
//DELINEATE = "describe or portray something precisely"
//or "indicate the exact position of (a border or boundary)"
